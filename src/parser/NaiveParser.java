package parser;

import model.JSON;

public class NaiveParser implements JsonParser {
    @Override
    public JSON parse(String jsonText) {
        return null;
    }

    @Override
    public String toString(JSON json) {
        return null;
    }
}
